```

## 3.2.2. `git add`

Agrega un archivo al área de preparación (stage). Si el archivo es nuevo, se agrega por primera vez. Si el archivo ya estaba siendo versionado, se marca como modificado y se prepara para ser versionado.

```bash